a = 10
b = a
b = 20

print(a) 
print(b)
#Immutable objects cannot be changed after they are created. Examples: int, float, str, tuple.
# Mutable objects can be changed after they are created. Examples: list, dict, set